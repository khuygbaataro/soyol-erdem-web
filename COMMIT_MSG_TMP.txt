feat(admission): 8-step registration wizard + nav cleanups

Round 3 of Munkhchimeg's web edits.

Admission registration form (the headline change):
  * New /admission/register page — eight-step wizard (citizenship,
    degree, program, name, education, exam scores, phones, email).
  * Stepper at the top with progress bar; back-jump to completed steps
    is allowed, forward only after per-step validation passes.
  * Program dropdown (step 3) is filtered by the degree picked in
    step 2 so a master applicant doesn't see undergraduate programs.
  * Exam scores and phones are repeatable (1–3 entries each).
  * On submit we POST to a new /api/admission-applications endpoint
    that lands the application in the existing ContactSubmission table
    -- no Prisma migration needed, admin sees it under "Шинэ зурвас"
    with subject "Элсэлтийн анкет — <name>" and the structured
    fields formatted into a readable Markdown body.
  * All seven "Бүртгүүлэх / Хүсэлт илгээх / Шилжилт хүсэх / Онлайнаар
    бүртгүүлэх" CTAs in ADMISSION_PROGRAMS now point at /register
    instead of bouncing to /contact.

Programs page cleanup:
  * Drop the "Зэрэг бүгд / Хэл бүгд / Search" filter row per the
    editor's "Зэрэг бүгд, хэл бүгд сонголттой хэсгийг устгана" note.
  * Group remaining cards by degree (Бакалавр, Магистр, then anything
    else alphabetically) with a heading + count per group.

News category labels:
  * EVENT label "Үйл явдал" → "Нээлттэй ажлын байр".
  * ANNOUNCEMENT label "Зарлал" → "Ярилцлага".
  * Enum values stay the same so existing rows are unaffected; only
    the human label changed in NEWS_CATEGORY_LABEL and NewsForm's
    CATEGORY_OPTIONS.

Student life:
  * Drop the "Манай гэр бүлд нэгдээрэй" CtaBanner per editor's
    explicit "хэсгийг устгах" note.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
